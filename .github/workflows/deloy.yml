name: Deploy

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'README.md'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: curl https://api.ipify.org
      - run: set -eu
      - run: mkdir "$HOME/.ssh"
      - run: echo "${{ secrets.test }}" > "$HOME/.ssh/test"
      - run: chmod 0600 "$HOME/.ssh/test"
      - name: Install composer dependencies😒😂
        run: composer install
      - run: rsync --exclude-from=rsync_exclude.txt --del -e "ssh -p 22 -vvv -i $HOME/.ssh/test -o StrictHostKeyChecking=no" --archive --compress --delete . j01391837@maphr.ru:/home/users/j/j01391837/domains/maphr.ru
      - name: Generate encryption key
        run: php artisan key:generate
