name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      # Setup key328dfdfdgsgс
      - run: curl https://api.ipify.org
      - run: set -eu
      - run: mkdir "$HOME/.ssh"
      - run: echo "${{ secrets.test }}" > "$HOME/.ssh/test"
      - run: chmod 0600 "$HOME/.ssh/test"
      # Deploy
      - run: cd app && rsync -e "ssh -p 22 -vvv -i $HOME/.ssh/test -o StrictHostKeyChecking=no" --archive --compress --delete . j01391837@myjino.ru:/home/users/j/j01391837/domains/maphr.ru
